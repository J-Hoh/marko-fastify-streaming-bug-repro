style {
	html, body {
		width: 100%;
		height: 100%;
	}
	body {
		display: flex;
		flex-direction: column;
	}
	body > * {
		flex: 0 0 auto;
	}
}

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8"/>
		<${input.head}/>
	</head>
	<body>
		<sample-header title=`There's a bug in my code (${input.renderingServer})`/>
		<${input.renderBody}/>
	</body>
</html>